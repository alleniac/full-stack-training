{
  "name": "full-stack-training",
  "version": "1.0.0",
  "description": "This is a full-stack web app deployed on AWS, with React at frontend and Express at backend",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "server": "nodemon --ignore client/ server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "node server.js"
  },
  "keywords": [
    "AWS",
    "React",
    "Express"
  ],
  "author": "Allen Ma",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^6.3.0",
    "nodemon": "^2.0.13"
  }
}
